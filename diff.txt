diff --git a/README.md b/README.md
index 485b46c..5195b6c 100644
--- a/README.md
+++ b/README.md
@@ -1,334 +1,296 @@
 # man-translate
 
-`man-translate` — небольшая Bash-утилита для подготовки man-страниц GNU/Linux к переводу с помощью LLM и установки готового русского перевода в локальную man-базу.
+Утилита для перевода man-страниц GNU/Linux на русский язык с помощью
+LLM, с установкой перевода в систему и хранением переводов в git-репозитории.
 
-Скрипт:
-
-1. Находит исходную man-страницу.
-2. Извлекает её исходный roff-текст.
-3. Создаёт рабочий файл с prompt для LLM и текстом оригинала.
-4. Ожидает, пока пользователь заменит содержимое файла готовым переводом.
-5. Проверяет roff-разметку через `groff`.
-6. Сжимает перевод и устанавливает его в:
-   ```text
-   /usr/local/share/man/ru/man<секция>/
-   ```
-7. Обновляет базу `man` через `mandb`.
-
-Локальные переводы устанавливаются в `/usr/local/share/man`, а не в `/usr/share/man`, поэтому они не должны перезаписываться при обновлении системных пакетов.
+Скрипт **не переводит сам** — он готовит текст для передачи в LLM
+(ChatGPT, Claude и т.п.), проверяет результат, устанавливает его в
+систему и сохраняет в репозиторий, чтобы переводы не терялись при
+переустановке системы или смене компьютера.
 
 ---
 
 ## Возможности
 
-- подготовка man-страницы для перевода LLM;
-- поиск исходной страницы в locale `C`, чтобы не переводить уже установленный перевод;
-- поддержка несжатых man-файлов и архивов:
-  - `.gz`;
-  - `.xz`;
-  - `.bz2`;
-  - `.zst`;
-- проверка результата через `groff`;
-- проверка, что файл действительно был изменён;
-- защита от случайной установки файла с исходным prompt и служебными разделителями;
-- установка с корректными правами `0644`;
-- пользовательский prompt;
-- установленный системный prompt;
-- `--help` и `--version`.
+- Подготовка man-страницы к переводу: распаковка, добавление промпта
+  и служебных разделителей.
+- Поддержка сжатых страниц: `.gz`, `.xz`, `.bz2`, `.zst`.
+- Проверка результата: контроль изменения файла, проверка на остатки
+  служебных разделителей, валидация roff через `groff`.
+- Установка перевода в `/usr/local/share/man/ru/manN/` и обновление
+  базы `mandb`.
+- Сохранение несжатого перевода (чистый roff) в git-репозиторий.
+- Автоматический (по запросу) `git commit` и `git push`.
+- Массовая установка готовых переводов из репозитория при
+  разворачивании на новой машине.
 
 ---
 
-## Требования
-
-Скрипт рассчитан на GNU/Linux с `man-db`.
-
-Необходимы следующие программы:
-
-```text
-bash
-man
-groff
-gzip
-sha256sum
-mandb
-sudo
-mktemp
-install
-sed
-awk
-grep
-```
+## Зависимости
 
-Для man-страниц, сжатых альтернативными алгоритмами, могут также понадобиться:
+Обязательные:
 
-```text
-xz
-bzip2
-zstd
+```
+man groff gzip sha256sum mandb sudo mktemp install sed awk
 ```
 
-На Debian/Ubuntu базовые зависимости обычно можно установить так:
+Для соответствующих форматов сжатия дополнительно:
 
-```bash
-sudo apt install man-db groff gzip coreutils
+```
+xz bzip2 zstd
 ```
 
-На Arch Linux:
+Для сохранения в репозиторий и авто-commit:
 
-```bash
-sudo pacman -S man-db groff gzip coreutils
+```
+git
 ```
 
 ---
 
 ## Установка
 
-Клонируйте репозиторий:
-
-```bash
-git clone https://github.com/mirninec/man-translate.git
-```
-
-Перейдите в каталог проекта:
-
-```bash
+```sh
+git clone https://github.com/mirninec/man-translate.git man-translate
 cd man-translate
-```
-
-Сделайте установщик исполняемым:
-
-```bash
-chmod +x install.sh
-```
-
-Установите программу:
-
-```bash
 ./install.sh
 ```
 
-Установщик разместит файлы в следующих местах:
+`install.sh`:
 
-```text
-/usr/local/bin/man-translate
-/usr/local/share/man-translate/prompt.txt
-```
+1. Устанавливает программу в `/usr/local/bin/man-translate`.
+2. Устанавливает промпт в `/usr/local/share/man-translate/prompt.txt`.
+3. Предлагает записать путь к репозиторию в конфиг
+   (см. «Настройка репозитория»).
+4. Если в репозитории есть готовые переводы — предлагает установить
+   их в систему (см. «Восстановление переводов»).
 
-После этого команду можно запускать из любого каталога:
+---
 
-```bash
-man-translate scp
-```
+## Настройка репозитория
 
----
+Чтобы установленная команда `man-translate` знала, куда сохранять
+переводы, ей нужен путь к клону этого репозитория.
 
-## Использование
+Путь определяется по приоритету:
 
-Общий вид команды:
+1. Переменная окружения `MAN_TRANSLATE_REPO`.
+2. Строка `REPO=...` в `~/.config/man-translate/config`.
+3. Каталог `translations` рядом со скриптом
+   (актуально только при запуске `./man-translate.sh` из клона
+   до установки).
 
-```bash
-man-translate <имя-man-страницы>
-```
+### Автоматическая настройка
 
-Примеры:
+При запуске `./install.sh` скрипт предложит записать путь к текущему
+репозиторию в `~/.config/man-translate/config`. Это рекомендуемый
+способ: он не зависит от shell и не требует перезапуска сессии.
 
-```bash
-man-translate pwd
-man-translate scp
-man-translate printf
-man-translate rpc
-```
+### Ручная настройка
 
-Справка:
+Через конфиг:
 
-```bash
-man-translate --help
+```sh
+mkdir -p ~/.config/man-translate
+echo "REPO=$HOME/man-translate" > ~/.config/man-translate/config
 ```
 
-Версия:
+Либо через переменную окружения (например, в `~/.bashrc`):
 
-```bash
-man-translate --version
+```sh
+export MAN_TRANSLATE_REPO="$HOME/man-translate"
 ```
 
----
+Переменная окружения имеет наивысший приоритет и удобна для
+временного переопределения репозитория.
 
-## Процесс перевода
+---
 
-Например, для перевода страницы `scp`:
+## Использование
 
-```bash
-man-translate scp
+```sh
+man-translate <сущность>
 ```
 
-Скрипт найдёт исходную страницу и создаст рабочий файл, например:
+Примеры:
 
-```text
-~/man-ru-translate/scp.1.ru
+```sh
+man-translate scp
+man-translate printf
+man-translate rpc
 ```
 
-Далее:
+### Пошаговый процесс
 
-1. Откройте созданный файл.
-2. Скопируйте его содержимое.
-3. Передайте текст LLM.
-4. Попросите LLM перевести man-страницу на русский язык с сохранением roff-разметки.
-5. Полностью замените содержимое рабочего файла ответом LLM.
-6. Вернитесь в терминал и нажмите `Enter`.
-7. Скрипт проверит roff-файл, сожмёт его, установит и обновит базу man-страниц.
+1. Скрипт находит английскую man-страницу и определяет её имя и
+   раздел.
+2. Создаёт файл `~/man-ru-translate/<имя>.<раздел>.ru`, содержащий
+   промпт, разделители и оригинальный roff-текст.
+3. Вы открываете файл, копируете его содержимое, передаёте LLM.
+4. **Полностью заменяете** содержимое файла ответом LLM
+   (без промпта и без разделителей) и сохраняете файл.
+5. Нажимаете ENTER. Скрипт:
+   - проверяет, что файл изменился;
+   - проверяет отсутствие служебных разделителей;
+   - валидирует roff через `groff`;
+   - сжимает и устанавливает страницу в систему;
+   - сохраняет несжатый перевод в репозиторий;
+   - предлагает закоммитить и запушить изменения;
+   - обновляет базу `mandb`.
 
-После успешной установки перевод будет находиться примерно здесь:
+Проверка результата:
 
-```text
-/usr/local/share/man/ru/man1/scp.1.gz
+```sh
+LANG=ru_RU.UTF-8 man <сущность>
 ```
 
-Проверка:
+### Прочие флаги
 
-```bash
-LANG=ru_RU.UTF-8 man scp
+```sh
+man-translate --help      # справка
+man-translate --version   # версия
 ```
 
 ---
 
-## Prompt для LLM
-
-По умолчанию `man-translate` ищет `prompt.txt` в следующем порядке:
-
-1. Пользовательский prompt:
+## Хранение переводов
 
-   ```text
-   ~/.config/man-translate/prompt.txt
-   ```
+Переводы хранятся в репозитории в каталоге `translations` в
+**несжатом** виде (чистый roff). Это удобно для git: диффы читаемы,
+переводы легко просматривать и поддерживать.
 
-2. `prompt.txt` рядом с запускаемым скриптом.
+Структура:
 
-   Это удобно при запуске из каталога разработки:
+```
+translations/
+└── ru/
+    ├── man1/
+    │   ├── scp.1
+    │   └── wc.1
+    └── man5/
+        └── rpc.5
+```
 
-   ```bash
-   ./man-translate.sh scp
-   ```
+Раздел определяется по имени каталога `manN`, а имя файла — как
+`<имя>.<раздел>` без сжатия.
 
-3. Системный prompt, установленный через `install.sh`:
+> Пустой каталог `translations/` можно зафиксировать в git с помощью
+> файла-заглушки `translations/.gitkeep`. Он не мешает: логика поиска
+> переводов рассматривает только файлы внутри `translations/<язык>/manN/`.
 
-   ```text
-   /usr/local/share/man-translate/prompt.txt
-   ```
+---
 
-4. Встроенный prompt.
+## Коммит и публикация переводов
 
-Чтобы использовать собственный prompt:
+После сохранения перевода в репозиторий `man-translate` предлагает:
 
-```bash
-mkdir -p ~/.config/man-translate
-cp prompt.txt ~/.config/man-translate/prompt.txt
 ```
-
-После этого отредактируйте файл:
-
-```bash
-nano ~/.config/man-translate/prompt.txt
+Закоммитить и запушить изменения? (y/N):
 ```
 
-Пользовательский prompt имеет наивысший приоритет.
+При согласии скрипт:
 
----
+- добавляет в индекс **только** файл нового перевода
+  (не `git add .`, чтобы не захватить посторонние правки);
+- создаёт коммит с сообщением
+  `Add translation: <имя>.<раздел>`;
+- выполняет `git push`.
 
-## Русская locale
+Если `git` не установлен, каталог не является git-репозиторием, или
+вы ответили «нет» — скрипт выведет команды для ручного выполнения.
 
-Для автоматического выбора русской man-страницы должна быть доступна русская locale:
+Ошибки `push` (нет remote, нет сети, требуется аутентификация) не
+прерывают работу: страница уже установлена и сохранена, а push можно
+повторить вручную:
 
-```bash
-locale -a | grep -i ru
+```sh
+git -C "$HOME/man-translate" push
 ```
 
-Обычно требуется:
+---
 
-```text
-ru_RU.utf8
-```
+## Восстановление переводов на новой машине
 
-Проверить, какой файл будет выбран:
+При переустановке системы или на новом компьютере:
 
-```bash
-LANG=ru_RU.UTF-8 man -aw scp
+```sh
+git clone https://github.com/mirninec/man-translate.git man-translate
+cd man-translate
+./install.sh
 ```
 
-После установки локального перевода первым должен отображаться путь вида:
+Если в репозитории есть каталог `translations` с переводами,
+`install.sh`:
 
-```text
-/usr/local/share/man/ru/man1/scp.1.gz
-```
+1. Покажет список найденных переводов.
+2. Спросит подтверждение на установку.
+3. Для каждого перевода проверит roff, сожмёт его и установит в
+   `/usr/local/share/man/ru/manN/`.
+4. Один раз обновит базу `mandb`.
 
-Если русский каталог man-страниц не выбирается автоматически для `ru_RU.UTF-8`, можно создать символические ссылки:
+Битые страницы (не проходящие проверку `groff`) пропускаются и не
+ломают систему `man`; в конце выводится сводка «установлено /
+пропущено».
 
-```bash
-sudo ln -s ru /usr/local/share/man/ru_RU
-sudo ln -s ru /usr/local/share/man/ru_RU.UTF-8
-sudo mandb -q
-```
+---
 
-Проверка:
+## Настройка промпта
 
-```bash
-LANG=ru_RU.UTF-8 man scp
-```
+Промпт для LLM выбирается по приоритету:
 
----
+1. Пользовательский: `~/.config/man-translate/prompt.txt`
+2. Рядом со скриптом: `<репозиторий>/prompt.txt`
+3. Системный: `/usr/local/share/man-translate/prompt.txt`
+4. Встроенный запасной (если ничего не найдено).
 
-## Структура проекта
+Чтобы использовать собственный промпт, поместите его в:
 
-```text
-.
-├── install.sh
-├── man-translate.sh
-└── prompt.txt
+```
+~/.config/man-translate/prompt.txt
 ```
 
-| Файл | Назначение |
-|---|---|
-| `man-translate.sh` | Основной скрипт подготовки, проверки и установки перевода. |
-| `install.sh` | Установщик программы в `/usr/local`. |
-| `prompt.txt` | Prompt для LLM, используемый при подготовке перевода. |
+Промпт по умолчанию инструктирует LLM сохранять разметку roff, не
+переводить команды, опции, пути, переменные и код, а также
+переводить стандартные заголовки разделов
+(`NAME → ИМЯ`, `DESCRIPTION → ОПИСАНИЕ` и т.д.).
 
 ---
 
-## Удаление
+## Пути и каталоги
 
-Удалить установленную программу и системный prompt:
+| Назначение                       | Путь                                          |
+|----------------------------------|-----------------------------------------------|
+| Установленная программа          | `/usr/local/bin/man-translate`                |
+| Системный промпт                 | `/usr/local/share/man-translate/prompt.txt`   |
+| Установленные русские страницы   | `/usr/local/share/man/ru/manN/`               |
+| Рабочий каталог (черновики)      | `~/man-ru-translate/`                          |
+| Конфиг                           | `~/.config/man-translate/config`              |
+| Пользовательский промпт          | `~/.config/man-translate/prompt.txt`          |
+| Хранилище переводов в репозитории| `<репозиторий>/translations/<язык>/manN/`     |
 
-```bash
-sudo rm -f /usr/local/bin/man-translate
-sudo rm -rf /usr/local/share/man-translate
-```
-
-Локально установленные переводы находятся в:
-
-```text
-/usr/local/share/man/ru/
-```
-
-Чтобы удалить все переводы, установленные этой утилитой:
-
-```bash
-sudo rm -rf /usr/local/share/man/ru
-sudo mandb -q
-```
+---
 
-Рабочие файлы переводов находятся в:
+## Типичный рабочий цикл
 
-```text
-~/man-ru-translate/
-```
+```sh
+# 1. Перевод и установка
+man-translate wc
+#    ... перевод через LLM, ENTER ...
+#    ... "Закоммитить и запушить изменения? (y/N):" -> y
 
-Их можно удалить отдельно:
+# 2. На другой машине — восстановление
+git clone <URL> man-translate
+cd man-translate
+./install.sh
+#    ... "Установить эти переводы в систему? (y/N):" -> y
 
-```bash
-rm -rf ~/man-ru-translate
+# 3. Проверка
+LANG=ru_RU.UTF-8 man wc
 ```
 
 ---
 
 ## Лицензия
 
-Проект распространяется по лицензии [MIT](LICENSE).
+См. файл [LICENSE](LICENSE).
+
diff --git a/install.sh b/install.sh
index 47615d2..5d2d0b9 100755
--- a/install.sh
+++ b/install.sh
@@ -86,6 +86,78 @@ echo "Prompt:"
 echo "    $SHARE_DIR/prompt.txt"
 
 
+#
+# Чтобы установленная команда man-translate знала, куда сохранять
+# переводы, ей нужен путь к этому репозиторию. Мы записываем его в
+# файл ~/.config/man-translate/config в виде строки:
+#     REPO=/полный/путь/к/репозиторию
+#
+# Этот способ предпочтительнее правки ~/.bashrc:
+#   • не зависит от используемого shell;
+#   • не требует перезапуска сеанса;
+#   • читается непосредственно man-translate.sh.
+#
+
+CONFIG_DIR="$HOME/.config/man-translate"
+CONFIG_FILE="$CONFIG_DIR/config"
+
+echo
+echo "================================================="
+echo "Настройка репозитория переводов"
+echo "================================================="
+echo
+echo "Каталог этого репозитория:"
+echo "  $SCRIPT_DIR"
+echo
+
+# Смотрим, не записан ли уже путь в конфиг.
+current_repo=""
+
+if [[ -f "$CONFIG_FILE" ]]; then
+    current_repo=$(
+        grep -E '^REPO=' "$CONFIG_FILE" \
+        | head -n1 \
+        | cut -d= -f2- \
+        | sed -e 's/^"//' -e 's/"$//'
+    )
+fi
+
+if [[ "$current_repo" == "$SCRIPT_DIR" ]]; then
+    echo "Репозиторий уже настроен в конфиге:"
+    echo "  $CONFIG_FILE"
+else
+    if [[ -n "$current_repo" ]]; then
+        echo "В конфиге указан другой репозиторий:"
+        echo "  $current_repo"
+        echo
+    fi
+
+    read -rp "Записать текущий путь в конфиг? (y/N): " cfg_answer
+
+    if [[ "$cfg_answer" == "y" ]]; then
+        mkdir -p "$CONFIG_DIR"
+
+        # Удаляем старую строку REPO= (если была) и пишем новую.
+        if [[ -f "$CONFIG_FILE" ]]; then
+            grep -v -E '^REPO=' "$CONFIG_FILE" > "$CONFIG_FILE.tmp" || true
+            mv -- "$CONFIG_FILE.tmp" "$CONFIG_FILE"
+        fi
+
+        echo "REPO=$SCRIPT_DIR" >> "$CONFIG_FILE"
+
+        echo
+        echo "Записано в конфиг:"
+        echo "  $CONFIG_FILE"
+        echo "  REPO=$SCRIPT_DIR"
+    else
+        echo "Конфиг не изменён."
+        echo
+        echo "Позже можно настроить вручную:"
+        echo "  echo 'REPO=$SCRIPT_DIR' > $CONFIG_FILE"
+    fi
+fi
+
+
 #
 # ============================================================
 # Установка готовых переводов из каталога translations
@@ -171,8 +243,8 @@ if [[ -d "$TRANSLATIONS_DIR" ]]; then
         translations+=("$f")
     done < <(
         find "$TRANSLATIONS_DIR" \
+            -mindepth 3 -maxdepth 3 \
             -type f \
-            -path '*/man*/*' \
             -print0 \
         | sort -z
     )
@@ -285,4 +357,3 @@ echo "Пользовательский prompt можно разместить з
 echo
 echo "    ~/.config/man-translate/prompt.txt"
 echo
-
diff --git a/man-translate.sh b/man-translate.sh
index 0873df5..5103b48 100644
--- a/man-translate.sh
+++ b/man-translate.sh
@@ -4,12 +4,12 @@
 #
 # Подготовка русской man-страницы для перевода LLM.
 #
-# Версия: 0.3
+# Версия: 0.4
 #
 
 set -euo pipefail
 
-VERSION="0.3"
+VERSION="0.4"
 
 WORKDIR="$HOME/man-ru-translate"
 LANG_DIR="/usr/local/share/man/ru"
@@ -18,17 +18,50 @@ SCRIPT_NAME="$(basename "$0")"
 SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
 
 #
-# Каталог с переводами внутри проекта (git-репозиторий).
+# Определение каталога репозитория с переводами.
 #
-# Сюда после успешной установки копируется несжатый перевод,
-# чтобы его можно было закоммитить и не потерять.
+# После установки скрипт лежит в /usr/local/bin и оторван
+# от git-клона, поэтому вычислять путь от SCRIPT_DIR нельзя.
+# Путь к репозиторию задаётся явно, по приоритету:
 #
-# ВАЖНО: этот путь имеет смысл только тогда, когда скрипт
-# запускается из клонированного репозитория. После установки
-# в /usr/local/bin рядом со скриптом каталога translations нет,
-# поэтому копирование выполняется только если каталог доступен.
+#   1. Переменная окружения MAN_TRANSLATE_REPO.
+#   2. Строка REPO=... в ~/.config/man-translate/config.
+#   3. Каталог translations рядом со скриптом
+#      (актуально при запуске прямо из клона до установки).
 #
-REPO_TRANSLATIONS_DIR="$SCRIPT_DIR/translations"
+CONFIG_FILE="$HOME/.config/man-translate/config"
+
+REPO_DIR=""
+
+if [[ -n "${MAN_TRANSLATE_REPO:-}" ]]; then
+    # Высший приоритет: переменная окружения.
+    REPO_DIR="$MAN_TRANSLATE_REPO"
+
+elif [[ -f "$CONFIG_FILE" ]]; then
+    # Читаем строку вида: REPO=/home/user/man-translate
+    # grep берёт первую строку REPO=, cut отрезает значение.
+    # Кавычки по краям значения (если есть) убираем через sed.
+    REPO_DIR=$(
+        grep -E '^REPO=' "$CONFIG_FILE" \
+        | head -n1 \
+        | cut -d= -f2- \
+        | sed -e 's/^"//' -e 's/"$//'
+    )
+fi
+
+# Запасной вариант: запуск из клона репозитория.
+if [[ -z "$REPO_DIR" && -d "$SCRIPT_DIR/translations" ]]; then
+    REPO_DIR="$SCRIPT_DIR"
+fi
+
+# Итоговый каталог с переводами внутри репозитория
+# (пустой, если репозиторий определить не удалось).
+if [[ -n "$REPO_DIR" ]]; then
+    REPO_TRANSLATIONS_DIR="$REPO_DIR/translations"
+else
+    REPO_TRANSLATIONS_DIR=""
+fi
+
 
 GREEN="\033[0;32m"
 YELLOW="\033[1;33m"
@@ -462,47 +495,129 @@ sudo install -m 0644 -- "$TMPFILE" "$TARGET"
 # Сохранение перевода в репозиторий проекта
 #
 # Копируем НЕсжатый перевод (чистый roff) в каталог
-# translations/ru/manN/имя.N внутри проекта.
+# translations/ru/manN/имя.N внутри репозитория.
 #
 # Хранение в несжатом виде удобно для git:
 #   - видны нормальные диффы;
 #   - переводы можно закоммитить и запушить;
 #   - при переустановке системы install.sh их восстановит.
 #
-# Копирование выполняется только если каталог translations
-# реально доступен для записи. Когда man-translate запущен
-# из /usr/local/bin (после установки), этого каталога рядом
-# нет — тогда шаг просто пропускается с предупреждением.
+# Путь к репозиторию определён выше (REPO_TRANSLATIONS_DIR).
+# Если он не задан — шаг пропускается с подсказкой,
+# как настроить репозиторий.
 #
 
-if [[ -d "$REPO_TRANSLATIONS_DIR" ]] || \
-   [[ -w "$SCRIPT_DIR" ]]
-then
+if [[ -n "$REPO_TRANSLATIONS_DIR" ]]; then
+
     REPO_TARGET_DIR="$REPO_TRANSLATIONS_DIR/ru/man$SECTION"
     REPO_TARGET="$REPO_TARGET_DIR/${NAME}.${SECTION}"
 
-    if mkdir -p "$REPO_TARGET_DIR" 2>/dev/null; then
+        if mkdir -p "$REPO_TARGET_DIR" 2>/dev/null; then
         # cp без sudo: репозиторий принадлежит пользователю.
         cp -- "$OUTFILE" "$REPO_TARGET"
 
         msg "Перевод сохранён в репозиторий:"
         echo "  $REPO_TARGET"
         echo
-        warn "Не забудьте закоммитить и запушить:"
-        echo "  git add translations/"
-        echo "  git commit -m 'Add translation: ${NAME}.${SECTION}'"
-        echo "  git push"
-        echo
+
+        #
+        # Предложение закоммитить и запушить изменения.
+        #
+        # Действуем осторожно:
+        #   - git должен быть установлен;
+        #   - REPO_DIR должен быть git-репозиторием;
+        #   - в индекс добавляем ТОЛЬКО файл перевода,
+        #     а не 'git add .' (чтобы не захватить чужие правки);
+        #   - ошибки push не роняют скрипт (set -e обходим).
+        #
+
+        git_hint()
+        {
+            # Показывается, когда авто-commit невозможен
+            # или пользователь отказался.
+            warn "Не забудьте закоммитить и запушить:"
+            echo "  cd $REPO_DIR"
+            echo "  git add translations/"
+            echo "  git commit -m 'Add translation: ${NAME}.${SECTION}'"
+            echo "  git push"
+            echo
+        }
+
+        if ! command -v git >/dev/null 2>&1; then
+            warn "git не установлен — изменения не закоммичены."
+            git_hint
+
+        elif ! git -C "$REPO_DIR" rev-parse --is-inside-work-tree \
+                >/dev/null 2>&1; then
+            warn "Каталог репозитория не является git-репозиторием:"
+            warn "  $REPO_DIR"
+            git_hint
+
+        else
+            read -rp "Закоммитить и запушить изменения? (y/N): " git_answer
+
+            if [[ "$git_answer" == "y" ]]; then
+
+                msg "Коммит изменений..."
+
+                # Добавляем только этот файл перевода.
+                # git -C выполняет команду в каталоге репозитория,
+                # не меняя текущий каталог скрипта.
+                if git -C "$REPO_DIR" add -- "$REPO_TARGET"; then
+
+                    # Коммитим. Если коммитить нечего
+                    # (файл не изменился), git commit вернёт
+                    # ненулевой код — обрабатываем это мягко.
+                    if git -C "$REPO_DIR" commit \
+                        -m "Add translation: ${NAME}.${SECTION}"
+                    then
+                        msg "Коммит создан."
+
+                        msg "Push..."
+
+                        if git -C "$REPO_DIR" push; then
+                            msg "Изменения запушены."
+                        else
+                            warn "Не удалось выполнить push."
+                            warn "Возможные причины: нет remote,"
+                            warn "нет сети или требуется аутентификация."
+                            warn "Выполните вручную: git -C $REPO_DIR push"
+                        fi
+                    else
+                        warn "Коммит не создан"
+                        warn "(возможно, нечего коммитить)."
+                    fi
+                else
+                    error "Не удалось выполнить git add."
+                    git_hint
+                fi
+            else
+                git_hint
+            fi
+        fi
     else
-        warn "Не удалось записать перевод в репозиторий проекта."
-        warn "Каталог: $REPO_TARGET_DIR"
+        warn "Не удалось записать перевод в репозиторий:"
+        warn "  $REPO_TARGET_DIR"
+        warn "Проверьте права доступа к каталогу репозитория."
     fi
+
 else
-    warn "Каталог translations недоступен (запуск не из репозитория)."
-    warn "Перевод в репозиторий не сохранён — только установлен в систему."
+    warn "Репозиторий переводов не настроен —"
+    warn "перевод только установлен в систему, но не сохранён в git."
+    echo
+    warn "Чтобы переводы сохранялись автоматически, укажите путь"
+    warn "к клону репозитория одним из способов:"
+    echo
+    echo "  1) Переменная окружения (например, в ~/.bashrc):"
+    echo "       export MAN_TRANSLATE_REPO=\"$HOME/man-translate\""
+    echo
+    echo "  2) Файл конфигурации ~/.config/man-translate/config:"
+    echo "       REPO=$HOME/man-translate"
+    echo
 fi
 
 
+
 #
 # Обновление базы man
 #
